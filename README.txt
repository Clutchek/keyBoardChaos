This program is best suited for resolutions higher than 1440x960.

The application uses Gradle as a build program.

The tests for some reason do only seem to work (in Eclipse at least) 
when you used the test folder as source. After that you had to 
update the imports.